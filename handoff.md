# Handoff
## Completed: Transition accounting fix (bounds-inclusive room identity was the defect) + scenario v6 walk-first timeline
## Next Task: Restart OpenCode (v6 catalog), then run coop-transition-west v6 officially and retry coop-route-25; remaining combat/drop/save/soak gates
## Context: Root cause found and fixed: ManagedRoomKey equality included camera bounds, so door-scroll bounds churn minted fresh "rooms" and each stabilized variant counted a completion plus an abandoned post-transition window. The movement-session reducer now detects room changes by stage/area/room identity (ManagedRoomKey.SameRoomAs) and refreshes bounds silently within a room. Two regression tests added; full co-op gate green (26/26 movement session). Live-verified via reload_mod: east walk and west hop crossings each gave exactly completed+1/passed+1/9px/moved/settled/zero abandonments, including the continuous-P2-hold case that previously produced +3. Scenario bumped to v6: P1 walks into the threshold step first (Left 60, Left+Cross 12, Left 55, equal 252-frame ports) after the v5 hop-from-distance landed short against the step live. MCP tool timeout raised to 180s in opencode config (scenario runs ~35s).
## Files Modified: source/ManagedReplayState.cs (SameRoomAs), source/ManagedMovementSessionState.cs (identity-based room change/completion guards), .validation/CoopMovementSession/Program.cs (regression tests), DEVPLAN.md, handoff.md, README.md (v6 timeline doc)
