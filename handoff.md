# Handoff
## Completed: Native-load bootstrap live-verified through 56df1c6; scenario v6 + three keyboard route campaigns passed
## Next Task: Curated combat/drop, save/reload, and soak gates
## Context: The official v6 transition scenario passed with verified cleanup. Three independent physical-keyboard `coop-route-25` campaigns accepted all 25 140/220 transitions in 373, 330, and 321.5981476 seconds, completing the required route evidence with exact completion/pass/reconstruction/current-window movement evidence, zero abandonment or reconstruction failure deltas, and verified cleanup. The third artifact is `campaign-20260821T014103613Z-3b409ee856d06450`, from process 985721, session `62bd6e2a0422451f81ae8bf03ff07cec`, diagnostic generation 0, parent build `0.2.0+2206c03ca0e46280e244ed5c2815d99376ce402a`, and co-op `0.4.0` including native-load bootstrap through `56df1c6`; cleanup was attempted, succeeded, and verified. No other M5 gate is claimed. Subsequent campaign debugging corrected the parent observer's treatment of `postTransitionCommandedPixels`: it is intentionally per-window and resets to zero at completion, so campaign acceptance checks current pixels >=8; the parent regression suite covers reset-before-pass. The reducer both identifies rooms by stage/area/room (not scroll bounds) and waits for bounds to settle before reconstruction; live east/west crossings remain exact with zero transition failures. The prior `ce961a9` pending claim is superseded by live verification through `56df1c6`: file select was observed; selected-save progression armed bootstrap; stale room 44 selected provisionally; room 140 stayed nonsemantic with `transitionPending=false` and `transitionReconstructionFailures=0` during retries; stable room 140 selected closed bootstrap as `CHANGED_IDENTITY`. The subsequent `140 -> 220` crossing was a normal exact completed +1/pass +1 with 9 pixels and zero abandonment or transition reconstruction failure. At llvmpipe's roughly 17fps, XTest holds must be wall-clock scaled: west Left 3s + Z 0.3s + Left 3s, east Right 5s, and P2 inward 2s.
## Files Modified: DEVPLAN.md, README.md, handoff.md; third route campaign recorded
