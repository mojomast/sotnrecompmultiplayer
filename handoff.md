# Handoff
## Completed: Scenario v6 + two independent 25-transition keyboard route campaigns passed
## Next Task: One more independent route campaign, then curated contact/projectile/drop, save/reload, and soak gates
## Context: The official v6 transition scenario passed with verified cleanup. Two independent physical-keyboard `coop-route-25` campaigns accepted all 25 140/220 transitions, in 373 and 330 seconds, with exact completion/pass/reconstruction/current-window movement evidence, zero abandonment or reconstruction failure deltas, and verified cleanup. Subsequent campaign debugging corrected the parent observer's treatment of `postTransitionCommandedPixels`: it is intentionally per-window and resets to zero at completion, so campaign acceptance now checks current pixels >=8; the parent regression suite covers reset-before-pass. The reducer now both identifies rooms by stage/area/room (not scroll bounds) and waits for bounds to settle before reconstruction; live east/west crossings remain exact with zero transition failures. `ce961a9`'s player-load initial-layer behavior has focused regression coverage but remains pending live verification. At llvmpipe's roughly 17fps, XTest holds must be wall-clock scaled: west Left 3s + Z 0.3s + Left 3s, east Right 5s, and P2 inward 2s.
## Files Modified: DEVPLAN.md, README.md, handoff.md; prior source/validation commits are 8c91458 and 286fbda
