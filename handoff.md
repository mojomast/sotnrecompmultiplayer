# Handoff
## Completed: Live route campaign investigation; transition-accounting defect found
## Next Task: Fix movement-session reducer transition-episode dedupe (one completion per physical crossing), regression-test it, then rerun transition-west v5 and coop-route-25
## Context: Route v2 constants and the v5 hop timeline are live-verified and the campaign observer works, but six route attempts failed on transition metrics: the reducer counts multiple completions per door crossing (+1/+2/+3 varying with engine event timing and session state) and abandonments flap likewise. A manual post-completion long P2 hold produced the full target metric set once (completed+1, passed+1, 9px, moved, settled, no abandonment), so the doorway is passable; the counters are not truthful. Pad 2 keyboard via host Keys2 (I/J/K/L/U/O/P in settings.json) plus XTest ctypes delivery (/tmp/opencode/xkeys.py) drives real non-automation input and latches configuredProcessedPad2Available persistently. Patched card spawns room 140 (stage 0, cell 32,26). Campaign cleanup verified on every failed run. Parent 149/149, co-op gate green.
## Files Modified: DEVPLAN/handoff only this commit; prior commits carry route v2, scenario v5, and catalog/test updates
