# Handoff
## Completed: Live room identity discovery and route v2 correction
## Next Task: Run transition-west v4 and the coop-route-25 campaign live (physical/keyboard input only while a campaign holds the lease), then remaining combat/drop/save/soak gates
## Context: A patched sample memory card (US BASLUS-00067 slot, stage field 0x228, cell 0x22C/0x22E per SaveData/SaveInfo layout) now spawns Alucard in Marble Gallery. Live telemetry proved room/area bytes are the global room-table offset at 0x801375BC (NO0 index i = 0x27E4 + 8*i), so route v1 constants (area 0, room 9) could never match. Route v2 is the live-verified flat ping-pong between clock junction room 140 (index 21, cell 32,27) and save room 220 (index 31, cell 31,27), cyclic orderedRooms [140,220,140], fingerprint b1f6e989ccc0cc1484851761c2d8143d1891fafc494a04e1d6be024046f82896. Transition-west v4 and combat probes now predicate area 40 with rooms 140/52. West corridor rooms 52/12 live-verified; its stairs descend into rooms 3/2 with poison Slinger juggling, so the corridor stays excluded. Upper clock cell index 9 never registered as current room live. Validation: parent 149/149, co-op gate green (242 PASS lines incl. route manifest/aggregate), current/pinned 284160-byte assemblies.
## Files Modified: route manifest v2, route aggregate constants, campaign catalog/JSON/preflight, scenario catalog versions and five scenario JSONs, tests, README/DEVPLAN/release-matrix/docs
