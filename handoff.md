# Handoff
## Completed: Combat publication/observer fixes and bounded retry live observation
## Next Task: Run full validation separately, then repeat curated combat/drop; save/reload and soak remain
## Context: Contact/projectile scenario v4 is reset-none because reset invalidates curated P2 placement. Native attack publication is camera-locked screen-space; contact uses standing-body offset Y 1 and half-height 20; generation ordering and cleanup deferral preserve the exact-owned entity through the next HitDetection. One-shot attribution requires exactly one pre-captured target, its slot becoming empty, EXP increasing, and exactly one kill increment; native EXP is read-only at status offset 0x288. Live attacks killed the target and native EXP/kills advanced, but observer defects were fixed during the run and no accepted contact/projectile/drop artifact exists after the final fix because room 52 was unstable and exact-one target activation could not be held. A separate unsupported-terrain trace showed one failed reconstruction, 29 suppressions, one retry, then Selected recovery with cooldown clear and no fatal, so only the bounded post-retry policy is live-observed. The deterministic private setup card was not committed. M5 remains In progress.
## Files Modified: README.md, DEVPLAN.md, handoff.md; parent handoff.md
